#Shutil Module, it is used to perform high level file operations
import shutil

"""
shutil.copy("src", "dest") : used to copy cmd

shutil.copy2("src", "dest") : used to copy files but with metadata

shutil.copytree("src", "dest") : Used to copy directories recursively

shutil.move("src", "dest") : Move the file

shutil.rmtree("src") : used to remove a directory
"""
